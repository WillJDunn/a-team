//api/boards/:boardId/user/:username/POST:Add an already existing user to an already existing board
// INSERT INTO teama.board_users (project_id, user_id, is_admin, added_by, added_at)
//VALUES (in_project_id, in_user_id, in_is_admin, in_added_by, in_added_at);
